package com.gurubelli.surya.concurrency;

public class MyImmutable {

	
	//Safe publication 
	/*
	 * Initializing an object reference from static initializer
	 * Storing a reference to it into volatile filed or AtomicReference
	 * Storing a reference to it into final field or properly constructed object 	
	 * Storing a reference to it into field that is 
	 * 
	 * Thread-confined :
	 * Shared read only : (immutable and effectively immutable)
	 * Shared thread-safe ( A thread safe object performs synchronization internally)
	 * Guarded : 
	 * 
	 *
	 */
}




 